# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~

# Home page
GET        /                                                          controllers.Application.index
GET        /ligues                                                    controllers.Application.ligues

# Ligue
GET        /ligues/:shortName                                          controllers.Application.ligue(shortName)


# Comite
GET        /ligues/:ligueShortName/comites/:comiteShortName             controllers.Application.comite(ligueShortName, comiteShortName)

# Rank
GET        /ligues/:ligueShortName/comites/:comiteShortName/team        controllers.Classements.team(ligueShortName, comiteShortName)

# Map static resources from the /public folder to the /assets URL path
GET        /assets/*file                                              controllers.Assets.at(path="/public", file)
